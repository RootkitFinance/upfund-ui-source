export class CalculatorInfo {
    public subFloor: string
    public excess: string

    constructor(subFloor: string, excess: string) {
        this.subFloor = subFloor
        this.excess = excess
    }
}